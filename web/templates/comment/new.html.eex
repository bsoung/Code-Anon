<%= form_for @changeset, comment_path(@conn, :create), fn f -> %>
	<div class="form-group">
		<%= text_input f, :content, placeholder: "Add a comment", class: "form-control" %>
		<%= error_tag f, :content %>
	</div>

	<%= submit "Save Comment", class: "btn btn-primary" %>
<% end %>